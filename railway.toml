[build]
builder = "NIXPACKS"
watch = ["backend/**"]
# This tells Railway that the actual application code is inside the backend folder
root = "backend"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
